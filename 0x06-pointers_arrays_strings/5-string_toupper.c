#include "main.h"
#include <string.h>
#include <ctype.h>

/**
 * *string_toupper - reversing the received array
 * @*: the char
 *
 * Return: 0;
 */
char *string_toupper(char *)
{
	int i = 0;
	
	while (*[i] != '\0')
	{
		toupper(*[i]);
		i++;
	}
}

#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints a to z'
 *
 * Return: 0;
 */
void print_numbers(void)
{
	char i;
	for (i = 0; i < 10; i++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
